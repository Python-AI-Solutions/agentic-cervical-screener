apiVersion: v1
kind: Service
metadata:
  name: cervical-ai-viewer
  namespace: default
  labels:
    app: cervical-ai-viewer
spec:
  type: ClusterIP
  selector:
    app: cervical-ai-viewer
  ports:
    - port: 80
      targetPort: 8000
      name: http
