apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  - pdb.yaml
  - ingress-route.yaml
  - middlewares.yaml

commonLabels:
  app: cervical-ai-viewer
  managed-by: kustomize
